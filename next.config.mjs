/* eslint-disable import/no-anonymous-default-export */
export default {
  reactStrictMode: true,
  images: {
    domains: ['your-image-domain.com'],
  },
  // Add rewrites, redirects, etc. if needed
};